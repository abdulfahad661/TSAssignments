// Declaring a variable for your favorite movie
let favoriteMovie: string = "Inception";

// Template string to introduce yourself
let myName: string = "Fahad"; // Renamed from 'name' to 'myName' to avoid conflicts
let age: number = 30;
let introduction: string = `Hello, my name is ${myName} and I am ${age} years old.`;

// Current mood as a boolean
const isHappy: boolean = true;

// Combining first and last names
let firstName: string = "John";
let lastName: string = "Doe";
let fullName: string = `${firstName} ${lastName}`;

// User's age
let userAge: number = 25;

// Displaying product information
let productName: string = "Laptop";
let productPrice: number = 999.99;
let productInfo: string = `The product is a ${productName} and it costs $${productPrice}.`;

// Number of pets
let numberOfPets: number = 2;

// Checking if it is your birthday
let isMyBirthday: boolean = false;

// User information
let userInfo: string = `Name: ${firstName} ${lastName}, Age: ${userAge}`;

// Birth year
const birthYear: number = 1993;

// Full address from city and country
let city: string = "Dubai";
let country: string = "UAE";
let fullAddress: string = `${city}, ${country}`;

// Today's temperature
let temperature: number = 23.5;

// Logging all outputs
console.log(introduction, fullName, productInfo, userInfo, fullAddress);
